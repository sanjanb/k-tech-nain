# QR Payment Feature - Flow Diagram

## 🔄 User Flows

```
┌─────────────────────────────────────────────────────────────────┐
│                    FARMER REGISTRATION FLOW                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Register   │ ───▶ │   Profile    │ ───▶ │  List        │
│   Account    │      │   Created    │      │  Products    │
│ (No Payment) │      │  (Optional)  │      │ (No Payment) │
└──────────────┘      └──────────────┘      └──────────────┘
                             │
                             │ (Optional)
                             ▼
                      ┌──────────────┐
                      │  Add Payment │
                      │  in Profile  │
                      │  (UPI + QR)  │
                      └──────────────┘
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│                 FARMER ADDS PAYMENT DETAILS                      │
└─────────────────────────────────────────────────────────────────┘

Go to Profile ───▶ Enable QR Payment ───▶ Enter Details ───▶ Save
      │                    │                     │               │
      │                    │                     ▼               │
      │                    │            ┌──────────────┐         │
      │                    └───────────▶│  UPI ID      │         │
      │                                 │  QR Upload   │         │
      │                                 └──────────────┘         │
      │                                                           │
      └───────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                         ┌────────────────────┐
                         │  Firestore Update  │
                         │  - upiId           │
                         │  - qrCodeUrl       │
                         └────────────────────┘
                                    │
                                    ▼
                         ┌────────────────────┐
                         │   QR Visible on    │
                         │   All Products     │
                         └────────────────────┘
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│                    BUYER VIEWS PRODUCT                           │
└─────────────────────────────────────────────────────────────────┘

Browse Products ───▶ Click Product ───▶ View Details
                                              │
                                              ▼
                                    ┌──────────────────┐
                                    │ Check Farmer has │
                                    │  Payment Info?   │
                                    └──────────────────┘
                                       │              │
                               YES ────┘              └──── NO
                                │                          │
                                ▼                          ▼
                    ┌────────────────────┐    ┌────────────────────┐
                    │  Show QR Section:  │    │  Show Fallback:    │
                    │  - QR Image        │    │  - Email only      │
                    │  - UPI ID (copy)   │    │  - Yellow warning  │
                    │  - Farmer name     │    │  "No QR provided"  │
                    │  - Disclaimer      │    └────────────────────┘
                    └────────────────────┘
                                │
                                ▼
                    ┌────────────────────┐
                    │  Buyer can:        │
                    │  - Scan QR         │
                    │  - Copy UPI ID     │
                    │  - Pay directly    │
                    └────────────────────┘
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│                   DATA FLOW ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────┘

┌────────────────┐                  ┌─────────────────┐
│  Farmer Profile│                  │  Firebase       │
│  /profile      │                  │  Storage        │
└────────────────┘                  └─────────────────┘
        │                                    ▲
        │ Upload QR                         │
        │                                   │
        ▼                                   │
┌────────────────┐                         │
│  Validation    │                         │
│  - UPI format  │                         │
│  - File type   │─────────────────────────┘
│  - File size   │           Upload
└────────────────┘
        │
        │ Valid
        ▼
┌────────────────────────────────────────────┐
│         Firestore: users/{userId}          │
│  ┌──────────────────────────────────────┐  │
│  │  upiId: "farmer123@paytm"            │  │
│  │  qrCodeUrl: "https://...image.png"   │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
                    │
                    │ Fetched when viewing product
                    ▼
┌────────────────────────────────────────────┐
│      Product Detail Page                   │
│  /product/[id]                             │
│  ┌──────────────────────────────────────┐  │
│  │  Fetch product → Get farmerId        │  │
│  │  Fetch farmer → Get payment details  │  │
│  │  Display QR if available             │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│                  VALIDATION FLOW                                 │
└─────────────────────────────────────────────────────────────────┘

User Inputs UPI ID: "farmer123@paytm"
        │
        ▼
┌────────────────┐
│  Validation    │
└────────────────┘
        │
        ├─▶ Contains @? ───────────────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Must contain @"                         │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ├─▶ Username >= 3 chars? ──────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Username too short"                     │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ├─▶ Bank >= 2 chars? ──────────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Bank handle too short"                  │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ├─▶ Valid characters? ─────────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Invalid characters"                     │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ▼
    ✅ VALID → Format & Save
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│                  FILE UPLOAD FLOW                                │
└─────────────────────────────────────────────────────────────────┘

User Selects File
        │
        ▼
┌────────────────┐
│  Validation    │
└────────────────┘
        │
        ├─▶ Type PNG/JPEG? ────────────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Must be PNG or JPEG"                    │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ├─▶ Size < 2MB? ───────────────────────────────▶ YES ─┐
        │                                                      │
        └─▶ NO ──▶ ❌ "Must be < 2MB"                          │
                                                              │
        ┌─────────────────────────────────────────────────────┘
        │
        ▼
    Upload to Firebase Storage
        │
        ▼
    Get Download URL
        │
        ▼
    Save URL to Firestore
        │
        ▼
    ✅ Success
```

---

## 🎯 Key Decision Points

### Farmer Profile:
```
Is QR Payment Enabled?
├─ NO → Show toggle, allow enabling
└─ YES
   ├─ Has payment data?
   │  ├─ YES → Show view mode (with edit button)
   │  └─ NO → Show edit mode
   └─ Editing?
      ├─ YES → Show input fields + save/cancel
      └─ NO → Show current values
```

### Product Page:
```
Does farmer have payment info?
├─ YES (upiId OR qrCodeUrl)
│  └─ Show QR section
│     ├─ Show QR image if qrCodeUrl exists
│     ├─ Show UPI ID if upiId exists
│     └─ Show copy button + disclaimer
└─ NO
   └─ Show fallback message
      └─ Display email only
```

### Payment Removal:
```
User unchecks "Enable QR Payment"
├─ Has existing payment data?
│  ├─ YES → Confirm removal
│  │  ├─ Confirmed → Clear data + save
│  │  └─ Cancelled → Re-enable toggle
│  └─ NO → Just disable
└─ Update Firestore
   └─ Set upiId = null, qrCodeUrl = null
```

---

## 📊 State Management

### Farmer Profile Component States:
```javascript
paymentEnabled: boolean      // Toggle state
editingPayment: boolean      // Edit mode active?
editUpiId: string           // Temp UPI value
qrCodeFile: File | null     // Selected QR file
uploadingQr: boolean        // Upload in progress?
paymentError: string        // Validation error
saving: boolean             // Save in progress?
```

### Conditional Rendering:
```
paymentEnabled === false
└─ Show only toggle checkbox

paymentEnabled === true && !editingPayment
└─ Show current payment data + Edit button

paymentEnabled === true && editingPayment
└─ Show input fields + Save/Cancel buttons
```

---

## 🔐 Security & Privacy

### Data Access Control:
```
Farmer can:
├─ ✅ Add own payment details
├─ ✅ Edit own payment details
├─ ✅ Remove own payment details
└─ ❌ Cannot modify other farmers' data

Buyer can:
├─ ✅ View farmer's public payment info
├─ ✅ Copy UPI ID
└─ ❌ Cannot edit any payment data

Platform:
├─ ❌ Does not store payment confirmations
├─ ❌ Does not track transactions
├─ ❌ Does not verify payments
└─ ✅ Only provides display of farmer's data
```

---

## 📱 Responsive Design

### Desktop (> 768px):
```
┌─────────────────────────────────┐
│  Payment Details (Optional)     │
├─────────────────────────────────┤
│  [ ] Enable QR Payment          │
│                                 │
│  UPI ID: [________________]     │
│  QR: [Choose File] [filename]   │
│                                 │
│  [Save] [Cancel]                │
└─────────────────────────────────┘
```

### Mobile (< 768px):
```
┌───────────────────────┐
│ Payment Details       │
│ (Optional)            │
├───────────────────────┤
│ [ ] Enable QR Payment │
│                       │
│ UPI ID:               │
│ [__________________]  │
│                       │
│ QR Code:              │
│ [Choose File]         │
│                       │
│ [Save]                │
│ [Cancel]              │
└───────────────────────┘
```

---

## 🎉 Complete Flow Example

### End-to-End Happy Path:

1. **Farmer Registration**
   - Signs up → No payment required → ✅ Account created

2. **List Products**
   - Adds products → No UPI required → ✅ Products listed

3. **Add Payment Later**
   - Goes to profile → Enables QR payment
   - Enters UPI: `farmer@paytm` → ✅ Validated
   - Uploads QR image → ✅ Uploaded to Storage
   - Saves → ✅ Firestore updated

4. **Buyer Discovers**
   - Browses products → Clicks farmer's product
   - Sees QR section → ✅ QR displayed
   - Copies UPI ID → ✅ Clipboard updated

5. **Direct Payment**
   - Opens payment app → Scans QR / Enters UPI
   - Completes payment → ✅ Directly to farmer
   - Platform not involved → ✅ Zero liability

6. **Deal Completion**
   - Express interest → Create deal → Confirm deal
   - Works with or without QR → ✅ No blocking

**Result: Successful implementation! 🎊**
